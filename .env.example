# =================================================================
# Plantilla de variables de entorno (.env.example)
# Copia este archivo como '.env' y completa los valores reales.
# No subas el archivo .env a Git (está en .gitignore).
# =================================================================

# --- Servidor ---
PORT=3000
NODE_ENV=development

# --- WhatsApp / Twilio (implementación actual del bot) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=
# Número del estudiante de prueba (formato whatsapp:+..., según Twilio)
STUDENT_NUMBER=
# Opcional: notificación al docente por WhatsApp
TEACHER_NUMBER=
# Opcional: token para verificar webhooks de Twilio/Meta si lo configuras
WEBHOOK_VERIFY_TOKEN=

# --- IA (Groq) — generación de preguntas de opción múltiple ---
GROQ_API_KEY=

# --- Migración prevista a Telegram (solo referencia; el código aún no usa estas variables) ---
# Cuando se migre, se usarán por ejemplo:
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_STUDENT_CHAT_ID=
# TELEGRAM_TEACHER_CHAT_ID=
# TELEGRAM_WEBHOOK_SECRET=

# --- Base de datos (reservado para futuras versiones) ---
# DATABASE_URL=
