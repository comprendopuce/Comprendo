# Copia como .env.local para desarrollo local

# Desarrollo local (servicios por separado)
NEXT_PUBLIC_API_URL=http://localhost:5253
NEXT_PUBLIC_BOT_API_URL=http://localhost:3001

# Despliegue unificado en Render (mismo dominio, nginx):
# No hace falta configurar nada — el build usa rutas relativas (/api, /bot).
